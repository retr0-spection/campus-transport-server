import request from 'supertest';

let server;

// beforeAll(() => {
//   server = app.listen(3000);
// });

// afterAll(() => {
//   server.close();
// });

describe('GET /', () => {
  it('should return Hello World', async () => {
  });
});
